programs = ["lev","strong", "skeleton","polish"]
#for p in programs:
#    print(p, len(p))
# the range in the for loop plus the len funnction and the 
# argument are to initialize the members of the list from 0, END 
for p in range(len(programs)):
    print(p, programs[p])
